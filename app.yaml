# Databricks Apps configuration
command: ["streamlit", "run", "app.py", "--server.port", "8501", "--server.address", "0.0.0.0", "--server.headless", "true", "--browser.gatherUsageStats", "false"]
env:
  - name: STREAMLIT_SERVER_MAX_UPLOAD_SIZE
    value: "200"
